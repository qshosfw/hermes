---
description: How to extend current protocol specs
---

1. Locate relevant `rfc` document inside `src/docs/content/docs/rfc/`.
2. Update mathematical definitions with corresponding latex blocks.
3. Make sure to reflect logic accurately in visualizer code (`hermesProtocol.ts`).
4. Re-run `node scripts/generate-pdf.js` manually to check formatting after major structural updates.
